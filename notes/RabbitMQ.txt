RabbitMQ是什么 ？
 RabbitMQ是一个在AMQP基础上完整的，可复用的企业消息系统。他遵循Mozilla Public License开源协议。

1：安装RabbitMQ需要先安装Erlang语言开发包。下载地址 http://www.erlang.org/download.html 在win7下安装Erlang最好默认安装。
      配置环境变量 ERLANG_HOME C:\Program Files (x86)\erl5.9 
      添加到PATH  %ERLANG_HOME%\bin;
		[erlang ******]
			18.3 版本在安装rabbitmq-plugins 时出现‘找不到指定的模块。crypto’，==++==
		[erlang ******]
	  
2：安装RabbitMQ 下载地址 http://www.rabbitmq.com/download.html  安装教程：http://www.rabbitmq.com/install-windows.html
      配置环境变量 RABBITMQ_BASE = C:\Program Files (x86)\RabbitMQ Server\rabbitmq_server-2.8.0
      添加到PATH %RABBITMQ_BASE%\sbin;
	  
3：进入%RABBITMQ_SERVER%\sbin 目录以管理员身份运行 rabbitmq-plugins.bat
rabbitmq-plugins.bat enable rabbitmq_management

4: 安装完成之后以管理员身份启动 rabbitmq-service.bat
rabbitmq-service.bat stop
rabbitmq-service.bat install
rabbitmq-service.bat start

5：浏览器访问localhost:55672  默认账号：guest  密码：guest



CMD返回错误:---乱码
解决方法:

检查以下两个环境变量配置:
ERLANG_HOME 
RABBITMQ_BASE

Rabbit MQ安装后无法成功启动
Rabbit MQ安装后无法成功启动

重新执行 安装命令:
rabbitmq-service.bat remove
rabbitmq-service.bat install
rabbitmq-service.bat start.
ok,成功.




rabbitmq guest login failed

If you want create a new user with admin grants:

rabbitmqctl add_user test test
rabbitmqctl set_user_tags test administrator
rabbitmqctl set_permissions -p / test ".*" ".*" ".*"


rabbitmqctl add_user mq_coco mq123456
rabbitmqctl set_user_tags mq_coco administrator
rabbitmqctl set_permissions -p / mq_coco ".*" ".*" ".*"






